spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

chaoslab:
  kubernetes:
    namespace: target-app
    config-path: ${KUBECONFIG:}
    context: ${KUBECONTEXT:}
  target:
    base-url: ${TARGET_BASE_URL}

logging:
  level:
    root: INFO

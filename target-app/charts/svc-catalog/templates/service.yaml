apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc-catalog.fullname" . }}
  labels:
    {{- include "svc-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "svc-catalog.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc-order.fullname" . }}
  labels:
    {{- include "svc-order.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "svc-order.selectorLabels" . | nindent 4 }}

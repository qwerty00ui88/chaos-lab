apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc-user.fullname" . }}
  labels:
    {{- include "svc-user.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "svc-user.selectorLabels" . | nindent 4 }}

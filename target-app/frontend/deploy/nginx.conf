server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # User 서비스
  location /api/user/ {
    proxy_pass http://svc-user/api/user/;
  }

  # Catalog 서비스
  location /api/catalog/ {
    proxy_pass http://svc-catalog/api/catalog/;
  }

  # Order 서비스
  location /api/order/ {
    proxy_pass http://svc-order/api/order/;
  }

  # React SPA 라우팅 지원
  location / {
    try_files $uri /index.html;
  }
}

